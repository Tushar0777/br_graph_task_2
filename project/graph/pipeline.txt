 graph = (
        GraphNormalizer()
        .process(graph)
    )

    graph=(
        GraphDeduplicator()
        .process(graph)
    )

    graph = (
        GraphValidator()
        .process(graph)
    )
    